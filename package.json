{
  "name": "fq-compressor-docs",
  "private": true,
  "version": "0.1.0",
  "description": "Documentation for fq-compressor - Next.js + Nextra 3.x",
  "scripts": {
    "docs:build": "cd docs/website && npm install && npm run build",
    "docs:dev": "cd docs/website && npm install && npm run dev",
    "docs:serve": "cd docs/website && npm run start"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0"
  }
}
